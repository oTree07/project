{
    "builds": [
        {
            "buildCommand": "otree devserver",
            "use": "@vercel/python"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "settings.py"
        }
    ]
}
